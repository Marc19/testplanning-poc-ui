import React from 'react'

const ListExperiments = () => {
    return (
        <div>
            Should create table like view to show experiments with their methods, and refresh button.
            CHECK TREE VIEW IN MATERIAL UI
        </div>
    )
}

export default ListExperiments